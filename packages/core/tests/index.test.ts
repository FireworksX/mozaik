// @ts-ignore
import { types } from '../build/mozaikjs'

describe('@mozaikjs/core', () => {
  describe('main api', () => {
    test('declareModel', () => {
      const root = types('root', {

      })
    })
  })
})
