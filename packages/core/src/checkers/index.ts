import { model } from './modelType'

import { string, number, boolean, maybe, array, enumeration, custom } from './types'

export default {
  model,
  custom,
  enumeration,
  string,
  number,
  boolean,
  maybe,
  array
}
